{
  "name": "fawn-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "FAWN web app — single-file vanilla JS PWA, no build step. See README.md.",
  "scripts": {
    "serve": "python -m http.server 8080"
  }
}
